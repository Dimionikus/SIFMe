<UserControl x:Class="SIFMe.Forms.browser_form"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SIFMe.Forms"
             xmlns:objects="clr-namespace:SIFMe.Objects"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"  Height="450" Width="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SIFMe;component/Styles/form_style.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border Margin="25,0,0,0" Width="25" Style="{StaticResource TopBorder}"></Border>
        <Border x:Name="Dragger" Width="50" Height="50" Style="{StaticResource Dragger}"></Border>
        <Border Name="BottomBorder" Grid.Column="2" Style="{StaticResource BotomBorder}"></Border>
        <Canvas Grid.Column="1" Background="AliceBlue"></Canvas>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <objects:datatag_list/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
